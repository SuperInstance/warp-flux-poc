[package]
name = "warp-flux-poc"
version = "0.1.0"
edition = "2021"
description = "Warp FLUX PoC — constraint execution plugin architecture demonstration"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
rand = "0.8"
